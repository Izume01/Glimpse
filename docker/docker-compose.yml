services:
  api:
    build:
      context: ..
      dockerfile: Dockerfile
    command: bun run index.ts
    ports:
      - "3000:3000"
    depends_on:
      - postgres
      - redis

  worker:
    build:
      context: ..
      dockerfile: Dockerfile
    command: bun run workers.ts
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: app
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6378:6379"

volumes:
  pg_data:
